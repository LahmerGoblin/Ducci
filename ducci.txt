
Jedes Element: Abstand zum rechten Nachbarn
Abstand != 0
Abstand == 0

Einlesen von args
jedes arg
casten in int, in array stopfen

while args alle null
1. Element in hilf speichern
if i + 1 >= args.length
Math.abs(args[i+1]-args[i]


main()
args parsen -> intArray
ducci(intArray)


ducci(int[])
wenn Array nicht null -> duccistep(int[])
wenn Array null

ducciseq()
    Wenn Array nicht null && (Gesamtsumme != konstant)
        tmp = int[0];
        duccistep([0]);
    Wenn Array null 

    Wenn Gesamtsumme konstant


duccistep(i)
    wenn i != int.length;
        i = math.abs(intArray[i]-intArray[i+1]);
    sysout;
    duccistep(i+1);
    wenn i == int.lenght
        i = math.abs(intArray[i] - tmp);
        sysout + \n;
    ;
